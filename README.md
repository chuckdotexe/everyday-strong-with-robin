# Everyday Strong with Robin

Website for Robin Fetter's independent in-home and virtual personal training practice.

## Local development

```bash
npm install
npm run dev
```

The site is currently configured for local preview. A static GitHub Pages build and live contact-form delivery will be added before public deployment.

## Production build

```bash
npm run build
```
